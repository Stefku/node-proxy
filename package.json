{
  "name": "proxy-server",
  "version": "1.0.0",
  "description": "proxy server that add basic auth header on every request",
  "repository": "https://github.com/Stefku/node-proxy",
  "main": "proxy-server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "btoa": "^1.1.2",
    "express": "^4.15.2",
    "http-proxy": "1.16.2",
    "prompt": "^1.0.0"
  }
}
